# 修复日志 — ctx-compression 启动崩溃类缺陷

> 本文件是"启动崩溃"这一类缺陷的**跨版本继承台账**。每个分支/版本都要在这里留下自己的判定（受影响 / 不受影响 / 已修），新分支开工前先读这里，禁止逐分支重复踩。
> 记录规则：一条缺陷一个 `D#` 编号；一条记录必须含「现象 / 根因 / 取证证据 / 影响面 / 修法 / 验证手段」。

---

## D1 — Host 插件入口顶层静态 import 兄弟包（启动崩溃本体）

**现象**：`dsh web` 启动即崩，`plugin tree failed to load`：

```
failed to import loader entry context-compression-improved-bundle
(dsh-context-compression-improved): Cannot find package
'dsh-context-compression-improved-runtime' imported from
C:\Users\joshua\.dsh\profiles\web\node_modules\dsh-context-compression-improved\
packages\selector\lib\index.js
code: 'ERR_MODULE_NOT_FOUND'
```

**根因**：仓库以 git 方式整仓安装 → pnpm 只装根 manifest 声明的依赖 → 根 manifest `dependencies` 为空 → 兄弟包 `...-runtime` 从未被安装 → `packages/selector/lib/index.js:3` 的**顶层静态 import** 在模块加载期就抛 `ERR_MODULE_NOT_FOUND`。级联：Host 插件入口未加载 → `settings.register('context-compression', ...)` 从未执行 → 客户端 `SettingsScopeSnapshot.writable` 停在初值 `false` → 设置面板全部 `disabled=""`（面板本身仍可见，因为 client bundle 由 `dsh.client` 元数据独立解析，与本崩溃无关）。

**取证证据（committed 产物，非工作区）**：

| 分支 | `packages/selector/lib/index.js` |
| --- | --- |
| `feat/ctx-preset-v2` @`455f74f` | **L3**: `import { CONTEXT_COMPRESSION_SETTINGS_NAMESPACE, ContextCompressionSettingsSchema } from "dsh-context-compression-improved-runtime";` ← 崩溃点 |
| `compat/0.1.5` @`d7c592d` | 无顶层 import；仅在 L140 `modulePath("...-runtime", import.meta.resolve("...-runtime"))` 惰性解析 |

**影响面**：`feat/ctx-preset-v2` **受影响（即崩溃本体）**。

**修法**：单包合并（R2），`src/index.ts` 顶层改同包相对导入 `./runtime/config.ts`。
**验证手段**：`lib/**/*.js` 全量 grep，只允许出现冻结的 provenance 字面量，不允许出现 `from "dsh-context-compression-improved-runtime"`。

**状态**：工作区已修（见 G1）。待提交。

---

## D2 — 根 manifest 缺 dsh 安装契约（"装了等于没装"）

**现象**：`dsh plugin add github:...#<branch>` 装完，profile 里既没有 bundle 行也没有可用入口。

**根因**：git 安装读**仓库根 manifest**。根 manifest 若没有 `main` / `exports` / `dsh.client.inject` / `dsh.bundle.patch`，DSH 无从挂载；若根 manifest 的 `name` 还是 workspace 私有名并带 `"private": true`，pnpm 装下来的就是个无入口的私有包。

**取证证据**：

| 分支 | 根 manifest |
| --- | --- |
| `feat/ctx-preset-v2` @`455f74f` | `name=dsh-context-compression-improved`、有 `main`/`exports`/`dsh`（`cd03575` 已修）；但 `dependencies` **为空**，`exports` 缺 `./pruner`、`./invariant` |
| `compat/0.1.5` @`d7c592d` | `name=dsh-context-compression-improved-workspace`、`private=true`、**无 `main`/`exports`/`dsh`/`dependencies`** ← 完全不可安装 |
| `main` / `baseline/pre-god-module-split` @`e337bf5` | 同 compat：`name=dsh-context-compression-selector-workspace`、`private=true`、无 `main`/`exports`/`dsh` |

**影响面**：`compat/0.1.5`、`baseline/pre-god-module-split`、`main` **全部受影响**；`feat/ctx-preset-v2` 部分受影响（依赖为空 + exports 不全）。

> 这一条解释了历史现象「`dsh plugin --profile web add github:drscrewdriver/dsh-context-compression-improved#baseline/pre-god-module-split` 恢复基线后不太可用」：该分支是**拆分前的基线**，根 manifest 从未具备对外安装契约，且**没有任何 lib 产物入库**，`prepare` 又被 pnpm 的 allowBuilds 门禁拦截 —— 它按设计就不是可分发的。

**修法**：根 manifest 承担对外安装契约（`name`/`main`/`types`/`exports`/`dependencies`/`dsh`）；`dependencies` 写真实第三方依赖（`@huggingface/tokenizers@0.1.3`、`js-yaml@^4.2.0`）。
**验证手段**：`git show <branch>:package.json` 逐字段断言；真机 `dsh plugin add` 后 `node_modules/<pkg>` 存在且含 `lib/index.js`。

**状态**：`feat/ctx-preset-v2` 工作区已修（`./invariant` 导出仍缺，parity 项，不致病）。`compat/0.1.5` 未修（见 D5）。

---

## D3 — 构建产物 chunk 未入库（合并后新引入的"第二次同款崩溃"）

**现象**：合并后重新构建，`lib/index.js` 第一行变成 `import { n as CONTEXT_COMPRESSION_SETTINGS_NAMESPACE, r as ContextCompressionSettingsSchema } from "./config.js";`，`lib/invariant.js` 第一行为 `import { a as validatePublishedTailTrim, o as sessionEvents } from "./tail-trim.js";`。tsdown 把原 runtime 包内联代码拆成了**共享 chunk**。

**根因**：本仓库约定 `lib/` 提交进 git（pnpm 禁止 git 依赖带 `prepare`，只能在安装方消费已构建产物）。合并后新增产物 `lib/config.js`、`lib/tail-trim.js`、`lib/pruner.js`、`lib/pruner.d.ts`、`packages/selector/src/deepseek-v4-tokenizer.ts` **处于 untracked 状态**（`git ls-files packages/selector/lib` 只列 7 个旧文件）。若此时 push，GitHub 装下来的包里 `index.js` 引一个不存在的 `./config.js` → **同一个 `ERR_MODULE_NOT_FOUND`，换个文件名重演**。

**取证证据**：
- `Select-String packages\selector\lib\*.js -Pattern 'config\.js|tail-trim\.js'` → `index.js:1`、`invariant.js:1`、`pruner.js:1/2/110/118`
- `git ls-files packages/selector/lib` → 仅 `client.d.ts`、`client.js`、`index.d.ts`、`index.js`、`invariant.d.ts`、`invariant.js`、`style.css`
- 对历史提交的对照（说明这是**合并引入的新风险**，历史分支并未犯）：`feat/ctx-preset-v2`/`compat/0.1.5` 的 `packages/runtime/lib/tail-trim.js` 均已入库，V2 的 selector 产物内联、无相对 import。

**影响面**：**仅合并后的产物**（`feat/ctx-preset-v2` 工作区 + 未来的单包发布）。所有后续版本（V2 及其后继、compat 合并结果）一旦走单包结构都会继承这个风险 —— 因此本条必须作为**构建门禁**长期保留，而不是一次性修复。

**修法**：
1. 提交时把 `lib/**` 全量 `git add`（含 chunk 与新入口），`src/` 新文件一并入库；
2. 门禁：断言每个 entry 的每条相对 import 对应文件都在 `git ls-files` 中（防止下一位维护者再次漏 chunk）。

**验证手段（已落地为 CI 门禁）**：`scripts/verify-release.mjs` 新增产物图断言 —— 对 `lib/*.js` 提取 `from "./x"`，逐条断言 (a) 目标文件存在、(b) 被包 `files` 白名单覆盖（packed 安装路径）、(c) 被 git 跟踪（git 安装路径）。断言**按 import 图收口**：`lib/style.css` 这类无人 import 的构建副产物不纳入（其 CSS 规则已内联进 `lib/client.js` 的 `\0dsh-context-compression-css:` 区域，全仓无消费者），否则会逼迫为死产物扩白名单。

**实测证据**：
- 加断言前：`pnpm verify:release` → `release verification: OK`（**门禁漏检**，这就是当时可以直接 push 出去的原因）
- 加断言后：`Error: release verification: packages/selector/lib/config.js is not committed; a git install would fetch an incomplete artifact graph`
- `git ls-files` 为索引状态，故 `git add` 后即转绿，无需等提交。

**状态**：门禁已实现并实测（红→绿）；根因修复动作（`git add`）已随提交 `935d501` 落地并推送。

---

## D4 — 消费者 profile 侧配置残留与缺失

**现象**：即使包本身修好，profile 侧仍可能装不上或装错。

**取证证据（web profile 实测）**：
- `package.json.dependencies`：`dsh-context-compression-selector-workspace = github:...#baseline/pre-god-module-split`（**缺 `&path:/packages/selector`** → 解析到无入口的私有 workspace 根包）
- `pnpm-workspace.yaml.overrides`：`dsh-context-compression-improved-runtime → github:...#feat/ctx-preset-v2&path:/packages/runtime` —— 该路径**在合并后已不存在**，属于失效残留
- `pnpm-workspace.yaml.allowBuilds`：**缺 `@huggingface/tokenizers`**（缺它 `pnpm install` 直接被门禁拦）
- `node_modules` 实测只剩 `dsh-context-compression-selector-workspace`（= 没装成）

**影响面**：任何要装本插件的 profile（`web`、`web2`）。

**修法**：清三处残留 + 补 `allowBuilds`；安装地址必须带 `&path:`（双包时代）或改为单包后的仓库根地址。

**更正（实测推翻初判）**：`@huggingface/tokenizers@0.1.3` 的 `scripts` 只有 `clean`/`build`/`dev`/`lint`/`test`/`format`，**没有 `install`/`postinstall`**，`dist/` 随包发布 —— 因此它**不需要** `allowBuilds` 就能用。仓库自带的 `packed-install-e2e.mjs` 给消费者写的 `pnpm-workspace.yaml` 也只有 `packages: []`，没有 allowBuilds。故补 `allowBuilds` 属**预防性**动作，不是阻塞项；真正的阻塞项只有失效的 `overrides` 与错误的依赖 spec。

**状态**：未修（G4，按"给脚本、用户手工执行"交付）。

---

## 门禁证据（G2，2026-09-14 21:00 前后实测）

| 门禁 | 结果 | 说明 |
| --- | --- | --- |
| `pnpm build` | 通过 | 11 个产物，含 `config.js`/`tail-trim.js` chunk；无 `.map` |
| 跨包 import 扫描 | 通过 | `lib/**/*.js` 中 `from "dsh-context-compression-improved-runtime"` 命中 **0**；剩余 6 处均为冻结字面量 |
| `pnpm typecheck` | 通过 | 含 `tsconfig.tests.json` 严格测试门禁 |
| `pnpm test` | **4 failed / 354 passed / 1 skipped** | 与 T0.4 基线**逐项一致**（同为 2 个文件 4 条 `EPERM: rename …%TEMP%\dsh-context-compression-presets-*`），属环境型预存失败，合并**零新增失败** |
| `pnpm test:built` | 通过 | 1/1 |
| `pnpm verify:release` | 加门禁后按预期变红 | 见 D3：精确指向未提交的 `lib/config.js` |

---

## D5 — compat/0.1.5 分支落后于安装契约（继承判定：需要，且不止"合并"）

**判定**：**需要**。`compat/0.1.5` @`d7c592d` 比 `feat/ctx-preset-v2` 少 4 条提交（`0b20bf2`/`cd03575`/`4635d19`/`455f74f`），因此它继承的不应只是"单包合并"，而是**整套安装契约**：

1. 根 manifest 的对外契约（D2）：`name`/`main`/`types`/`exports`/`dsh`/`dependencies` —— compat 现在是 `dsh-context-compression-improved-workspace` + `private:true` + 全空，`dsh plugin add` 根本挂不上；
2. 单包合并（D1 的结构性消除）：compat 仍是双包，`selector/package.json` 未声明 `dsh-context-compression-improved-runtime` 依赖、`runtime` 只声明 `@huggingface/tokenizers`，git 直装无法把 runtime 装上 —— 与 V2 同病根（跨包 + pnpm 解析不到），只是表现不同：V2 是加载期崩，compat 是"没有 bundle 可加载所以看不出崩"；
3. 产物完整性门禁（D3）与 profile 配置（D4）。

**注意 compat 独有、不得被 V2 覆盖的改动**：0.1.5-rc.2 适配 —— client slot 名、`engines.dsh`、pinned overrides、lazy service resolution、estimator host-route、`/api` 前缀路由。故 Batch 7 保持"独立重放"而非 `git merge`。

**状态**：未开始（Batch 7）。

---

## 版本继承矩阵

| 分支 / 版本 | D1 顶层跨包 import | D2 安装契约 | D3 产物 chunk | D4 profile 配置 | 结论 |
| --- | --- | --- | --- | --- | --- |
| `feat/ctx-preset-v2` @`455f74f` | **有（崩溃）** | 部分（deps 空、exports 不全） | 无（合并前） | 需清 | 工作区已修 D1/D2，**D3 待闭** |
| 合并后单包（工作区，待提交） | 已消除 | 已修（`./invariant` 仍缺） | **有（待提交）** | 待做 | 本案 |
| `compat/0.1.5` @`d7c592d` | 无（惰性解析） | **完全缺失** | 无 | 待做 | **需继承整套**（Batch 7） |
| `baseline/pre-god-module-split` / `main` @`e337bf5` | 无 | **完全缺失 + 无 lib 入库** | n/a | 待做 | 设计上不可分发，勿作为回退点 |
| 本仓库未来的单包版本（≥0.1.0 发版） | 结构上不可再现 | — | **必须长期门禁** | — | 继承 D3 门禁 |

---

## 继承规则（给后续版本的最小必要动作）

1. **根 manifest 是对外安装契约**：`name`/`main`/`types`/`exports`/`dependencies`/`dsh` 六项齐全，`private` 视发布方式定；新增子路径导出（如 `./pruner`）两处 manifest 同步写。
2. **单包内不得出现跨包顶层 import**：`lib/**/*.js` grep 只允许冻结的 provenance 字面量。
3. **`lib/**` 必须与源码同提交**，且每个相对 import 的 chunk 都要入库 —— 门禁脚本化。
4. **provenance 字面量 `dsh-context-compression-improved-runtime` 永久冻结**：已写进历史 session 日志，改字面量会让历史 tail-trim 条目校验失败被丢弃。
5. **profile 侧三件套**：依赖地址带 `&path:`（或单包根地址）、清理失效 `overrides`、`allowBuilds` 打开 `@huggingface/tokenizers`。

---

## 变更记录

| 时间 | 条目 | 动作 |
| --- | --- | --- |
| 2026-09-14 | D1/D2/D3/D4/D5 | 建立台账；取证完成；D1/D2 工作区已修；D3 待提交；D5 待 Batch 7 |
| 2026-09-14 | D1/D2/D3 | **结案**：单包合并提交 `935d501` 并推送 `origin/feat/ctx-preset-v2`；`verify:release` 转绿 |

---

## 结案记录：`935d501`

**提交**：`935d501 refactor: merge runtime into the selector package for one-install git distribution`（父提交 `455f74f`）
**推送**：`455f74f..935d501 feat/ctx-preset-v2 -> feat/ctx-preset-v2`（`origin/feat/ctx-preset-v2` 已指向该提交）

**远端树实测（以 `origin/feat/ctx-preset-v2` 为准，非工作区）**：

```
packages/selector/lib/{client.d.ts, client.js, config.js, index.d.ts, index.js,
                       invariant.d.ts, invariant.js, pruner.d.ts, pruner.js,
                       style.css, tail-trim.js}          ← 11 个产物全部入库
lib/index.js:1  import { … } from "./config.js";          ← chunk 存在，可解析
root deps:      @huggingface/tokenizers=0.1.3; js-yaml=^4.2.0
root exports:   .  ./pruner  ./client  ./package.json
docs/repair-log.md 已入库
```

**资产完整性**：改名前后 blob hash 逐项一致（`deepseek-v4/tokenizer.json`、`tokenizer_config.json`、`manifest.json`、`LICENSE…txt`、`deepseek-v4-vision-exp/tokenizer.json` 均 `identical=True`），tokenizer 资产未被行尾归一化污染。

**仍未闭合的**：D4（profile 清理与装机，已交付脚本待手工执行）、D5（compat/0.1.5 继承，Batch 7）、`./invariant` 导出 parity 项。

---

## 路线 A 预验：git 直装在临时消费者目录端到端跑通（未触碰宿主）

在 `%TEMP%` 下新建一次性消费者目录（`package.json` + `packages: []` + `fetchTimeout: 600000`），执行真实安装：

```powershell
pnpm add "github:drscrewdriver/dsh-context-compression-improved#feat/ctx-preset-v2"
# → + dsh-context-compression-improved 0.1.0    Done in 1m 36.5s   exit 0
```

实测结论：

| 检查项 | 结果 |
| --- | --- |
| pnpm 接受根 manifest 带 `private: true` 的 git 依赖 | **接受**（`private` 只拦发布，不拦 git 安装） |
| 是否触发 `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED` | **未触发**（包只有 `prepack`，无 `prepare`） |
| 安装后 `packages/selector/lib/` 产物 | **11 个齐全**，含 `config.js`、`tail-trim.js`、`pruner.js` |
| `lib/index.js:1` 的 `./config.js` | 可解析（文件在位） |
| lib 内跨包静态 import | **0** 命中 |
| 传递依赖落位 | `@huggingface/tokenizers@0.1.3`、`js-yaml@4.3.2` 均已安装 |

另外对推送后的提交做了一次打包模拟（`git clone --branch feat/ctx-preset-v2` → `npm pack`）：tarball 内 `lib/` 11 个产物 + `dsh.plugin.json` + `cordis.patch.yml` + 两处 `package.json` 全部包含；6 个 shipped `.js` 逐一 `node --check` 通过，0 解析失败。

即：**git 安装路径已无剩余阻塞**，profile 侧只剩"清残留 + 换 spec"这一步。


