# DSH 上下文压缩插件启动崩溃 —— 单包修复交付说明与验收清单

- 日期：2026-09-14
- 对象：`dsh-context-compression-improved`（web profile 的 `context-compression-improved-bundle`）
- 仓库：`E:\test\rewrite-agently\mine-dsh-plugins\dsh-context-compression-improved`
- 计划包：`E:\test\rewrite-agently\.agents\plans\ctx-compression-single-package-merge\`（`spec.md` / `findings.md` / `tasks.md` / **`repair-log.md`**）

---

## 1. 崩溃根因（一句话）

仓库以 git 方式**整仓**安装 → pnpm 只装根 manifest 声明的依赖 → 根 manifest `dependencies` 为空 → 兄弟包 `dsh-context-compression-improved-runtime` 从未被安装 → `packages/selector/lib/index.js:3` 的**顶层静态 import** 在模块加载期抛 `ERR_MODULE_NOT_FOUND`。

级联：Host 插件入口未加载 → `settings.register('context-compression', ...)` 从未执行 → 客户端 `SettingsScopeSnapshot.writable` 停在初值 `false` → 设置面板全部 `disabled=""`（面板仍可见，因为 client bundle 由 `dsh.client` 元数据独立解析，与本崩溃无关）。

## 2. 修复状态

| 项 | 状态 | 证据 |
| --- | --- | --- |
| `packages/runtime` 全量并入 `packages/selector` | **已完成（工作区）** | `Test-Path packages\runtime` = False；`src/` 下已有 `runtime/`、`pruner.ts`、`invariant.ts` |
| 消除 entry 顶层跨包 import | **已完成** | `lib/**/*.js` 中 `from "dsh-context-compression-improved-runtime"` 命中 **0**；剩余 6 处全是冻结的 provenance 字面量 / `PACKAGE_NAME` / JSDoc |
| 根 manifest 安装契约 | **已完成** | 补 `dependencies`（`@huggingface/tokenizers@0.1.3`、`js-yaml@^4.2.0`）、`./pruner` 导出 |
| 产物图入库（洞 1） | **已提交并推送** | 提交 `935d501`，`455f74f..935d501 feat/ctx-preset-v2`；远端树实测 11 个产物齐全（含 `config.js`/`tail-trim.js`），`verify:release` 已转绿 |
| profile 清理与装机（洞 2） | **待你手工执行** | web profile 仍装着 `dsh-context-compression-selector-workspace`（缺 `&path:`），残留失效 `overrides` |

> **洞 1 是本轮最关键的发现**：合并后 tsdown 新拆出共享 chunk（`lib/index.js` 第一行 `import ... from "./config.js"`）。若直接 push，GitHub 装下来的包会在同一位置、以同一个 `ERR_MODULE_NOT_FOUND` 崩溃，只是文件名从 `-runtime` 变成 `./config.js`。

## 3. 门禁证据（本地已实测）

| 门禁 | 结果 |
| --- | --- |
| `pnpm build` | 通过，11 个产物（含 chunk），无 `.map` |
| `pnpm typecheck` | 通过（含 `tsconfig.tests.json` 严格测试门禁） |
| `pnpm test` | 4 failed / 354 passed / 1 skipped —— 与 T0.4 基线**逐项一致**（同为 `%TEMP%` 下 `EPERM: rename dsh-context-compression-presets-*`），属环境型预存失败，**合并零新增失败** |
| `pnpm test:built` | 通过 1/1 |
| `pnpm verify:release` | **已转绿** `release verification: OK`（`git add` 产物后；加断言前它在 chunk 缺失的情况下也报 OK —— 那就是漏洞本身） |

本轮还**新增了产物图门禁**（`scripts/verify-release.mjs`）：entry 的每条相对 import 必须 (a) 文件存在、(b) 被包 `files` 白名单覆盖、(c) 被 git 跟踪。加断言前 `verify:release` 在 chunk 缺失的情况下照样报 OK —— 这就是当时可以直接 push 出去的漏洞。

**仓库提交**：`935d501 refactor: merge runtime into the selector package for one-install git distribution`，已推送 `origin/feat/ctx-preset-v2`（`455f74f..935d501`）。远端树实测：11 个 lib 产物齐全、`lib/index.js:1` 的 `./config.js` 可解析、根 manifest 有 `dependencies` 与 `./pruner`；tokenizer 资产改名前后 blob hash 逐项一致。

**仓库内也落了修复记录**：`docs/repair-log.md`（D1–D5 跨版本台账 + 影响面矩阵 + 继承规则），并在 `CONTRIBUTING.md` 新增第 6 条要求开工前查阅。

## 4. 交付脚本（**由你手工执行**）

| 顺序 | 脚本 | 作用 |
| --- | --- | --- |
| 1 | `DSH-ccp-单包修复-01-提交并推送.ps1` | **已由我执行完毕**（提交 `935d501` 已推送）。脚本保留作复核与将来复用：前置断言 → `git add -A` → `verify:release` → 两次人工确认后 commit / push |
| 2 | `DSH-ccp-单包修复-02-装机与清理.ps1` | **待你手工执行**：备份 → 移除失效依赖 → 清 `overrides` → `dsh plugin --profile web add <spec>` → 安装结果逐项校验 |

```powershell
# 只需这一步（安装路径，按约定由你手工执行）
pwsh -File "E:\test\rewrite-agently\dsh-docs-deliverables\DSH-ccp-单包修复-02-装机与清理.ps1"
```

安装 spec（路线 A：github 直装）：`github:drscrewdriver/dsh-context-compression-improved#feat/ctx-preset-v2`
单包后不再需要 `&path:` —— 根 manifest 就是安装面。

两个脚本都**不重启 `dsh web`**：重启会中断你当前的 GUI 会话，时机由你定。

## 5. 重启后验收清单

1. 启动日志无 `plugin tree failed to load` / `ERR_MODULE_NOT_FOUND`
2. 设置面板出现 `context-compression` 分组，8 个模式按钮**可点选**（不再全体 `disabled`）
3. estimator 回归：provider 下拉可填充、模型名为空时可下拉、API key 自动匹配、且仍能自定义输入
4. 改一项设置 → 重启 → 能回读到刚才的值

## 6. 回滚

```powershell
Copy-Item "<profile>\package.json.bak-ccp-single-<stamp>" "<profile>\package.json" -Force
Copy-Item "<profile>\pnpm-workspace.yaml.bak-ccp-single-<stamp>" "<profile>\pnpm-workspace.yaml" -Force
# 然后在 profile 目录执行 pnpm install
```

## 7. 跨版本继承结论（本轮的第二个问题）

台账见 `repair-log.md`，缺陷编号 D1–D5。

| 分支 / 版本 | D1 顶层跨包 import | D2 安装契约 | D3 产物 chunk | 结论 |
| --- | --- | --- | --- | --- |
| `feat/ctx-preset-v2` @`455f74f` | **有（崩溃本体）** | 部分（deps 空、exports 不全） | 无 | **受影响**；工作区已修 D1/D2，D3 待提交 |
| `compat/0.1.5` @`d7c592d` | 无（惰性 `import.meta.resolve`） | **完全缺失** | 无 | **需继承整套**，见下 |
| `baseline/pre-god-module-split` / `main` @`e337bf5` | 无 | **缺失 + 零 lib 入库** | n/a | 设计上不可分发，**勿当回退点** |
| 合并后的单包版本 | 结构上不可再现 | 已修 | **必须长期门禁** | 继承 D3 门禁 |

**compat/0.1.5 要做什么**（已写入 `tasks.md` Batch 7）：
1. 它不是"只差合并"——比 V2 落后 4 条提交（`0b20bf2`/`cd03575`/`4635d19`/`455f74f`），根 manifest 还是 `dsh-context-compression-improved-workspace` + `private:true` + 无 `main`/`exports`/`dsh`/`dependencies`，`dsh plugin add` 根本挂不上；
2. 所以继承的是**整套安装契约**：根 manifest 对外契约 → 单包合并 → 产物图门禁 → profile 配置；
3. 它比 V2"少崩一次"只是因为 `selector` entry 用的是惰性 `import.meta.resolve`，表现为"没有 bundle 可加载"而非加载期崩溃 —— 同病根、异表征，不能因此判定它无需处理；
4. 保持**独立重放**而非 `git merge`：0.1.5-rc.2 独有适配（client slot 名、`engines.dsh`、pinned overrides、lazy service resolution、estimator host-route、`/api` 前缀路由）不得被带回旧线。

## 8. 未决 / 待确认

- 根 manifest `exports` 仍缺 `./invariant`（插件入口不依赖它，属 parity 项，不致病）。
- `lib/style.css` 是构建副产物：CSS 规则已内联进 `lib/client.js`，全仓无消费者，且被包 `files` 白名单排除（npm tarball 里没有它）。当前**未动**，留作 YAGNI 清理候选。
- 根 manifest 无 `files` 白名单 → git 安装会带上整仓（src/tests/scripts）。功能无碍，属体积优化候选。
- web2 profile 的 `dsh-context-compression-improved-workspace` allowBuilds 残留未清（不影响 web）。
